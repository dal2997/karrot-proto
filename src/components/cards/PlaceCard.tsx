

// PlaceCard: map에서 선택된 모임(장소/모임)을 보여주는 카드 역할.
// 현재 구현은 MapTab 내부 MeetingCard 컴포넌트를 그대로 쓰는 구조라,
// 필요하면 여기로 분리해서 MapTab에서 import 하도록 바꿀 수 있어요.

export default function PlaceCard() {
  return null;
}
